version: '3.9'
services:
  app: 
    build: .
    container_name: Smatal-Chatbot
    restart: always
    ports: 
      - "8080:8080"
    environment:
      REDIS_URL: ${REDIS_URL}
      GROQ_API_KEY: ${GROQ_API_KEY}
      GOOGLE_SHEET_CREDENTIALS_JSON: ${GOOGLE_SHEET_CREDENTIALS_JSON}
